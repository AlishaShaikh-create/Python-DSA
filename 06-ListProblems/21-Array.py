# array :
#  fixed size element
#  homogenous element

# python does not provide the built in array so we need to import the array array can be imported using the array and numpy

import array
arr=array.array('str',['a','b'])
print(arr)
# output:TypeError: 'float' object cannot be interpreted as an integer